<?xml version="1.0" encoding="UTF-8"?>
<project
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
  xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.yamcs.studio</groupId>
    <artifactId>org.yamcs.studio.releng</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>org.yamcs.studio.repository</artifactId>
  <packaging>eclipse-repository</packaging>

  <build>
    <plugins>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-director-plugin</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <products>
            <product>
              <id>yamcs-studio-runtime</id> <!-- 'uid' in the *.product -->
              <attachId>yamcs-studio-runtime</attachId>
              <rootFolder>yamcs-studio-runtime-${project.version}</rootFolder>
              <archiveFileName>yamcs-studio-runtime-${project.version}</archiveFileName>
            </product>
            <product>
              <id>yamcs-studio-editor</id> <!-- 'uid' in the *.product -->
              <attachId>yamcs-studio-editor</attachId>
              <rootFolder>yamcs-studio-editor-${project.version}</rootFolder>
              <archiveFileName>yamcs-studio-editor-${project.version}</archiveFileName>
            </product>
          </products>
          <formats>
            <linux>tar.gz</linux>
          </formats>
        </configuration>
        <executions>
          <execution>
            <id>create-distribution</id>
            <goals>
              <goal>materialize-products</goal>
              <goal>archive-products</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-repository-plugin</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <includeAllDependencies>true</includeAllDependencies>
          <repositoryName>Yamcs Studio</repositoryName>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
